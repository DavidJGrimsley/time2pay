import { CustomersProjectsScreen } from '@/features/settings/customers-projects-screen';
import { SettingsPageWidth } from '@/features/settings/settings-page-width';

export default function CustomersProjectsRoute() {
  return <SettingsPageWidth><CustomersProjectsScreen /></SettingsPageWidth>;
}
